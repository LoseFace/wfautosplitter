- A list of the template's groups (or missions) that have not yet been completed has been added to the overlay.
    - You can enable or disable this list in the overlay settings.
- For non-sequential recording mode, runs with different sequences are shown in different tables.
- The history table now includes a column showing the time for each group separately.
- Added checkboxes to enable or disable the display of splits, segments, and groups in the run history table.

- Fixed the display of split points for failed runs in the graph.